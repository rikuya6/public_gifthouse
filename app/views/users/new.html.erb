<% @page_title = 'Sing up' %>
<h1>アカウントの作成</h1>
<%= render 'shared/errors', obj: @user %>
<div>
  <%= form_for(@user) do |f| %>
    <%= render 'form', f: f %>

    <%= f.submit 'アカウントを作成する' %>
  <% end %>
</div>
